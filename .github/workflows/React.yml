
name: React Build

on:
  push:
    paths:
      - 'shsxctf/**'
 
     
jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    
    steps: 
      - name: Setup Docker Buildx
        uses: docker/setup-buildx-action@v1
      
      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_HUB_USERNAME }}
          password: ${{ secrets.DOCKER_HUB_PASSWORD }}

      - name: Build and push the Docker image
        uses: docker/build-push-action@v3
        with:
          push: true
          tags: |
            menisisaac/shsxctf-frontend:latest
            menisisaac/shsxctf-frontend:${{ github.sha }}
          context: "{{defaultContext}}:shsxctf"
          
  deploy:
    name: Deploy
    runs-on: self-hosted
    
    steps:
      - name: Remove existing containers
        run: docker rm $(docker stop $(docker ps -a -q --filter ancestor=menisisaac/shsxctf-frontend:latest --format="{{.ID}}"))
        
      - name: Pull latest images
        run: docker pull menisisaac/shsxctf-frontend:latest
        
      - name: Run images
        run: docker run -d -p 8080:80 menisisaac/shsxctf-frontend:latest
        
  



    
